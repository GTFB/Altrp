
import Page from "App/Models/Page";

// @ts-ignore
export default async function renderBreadcrumbs(settings, device, widgetId, randomString, {
  //page,
}: OptionsType) {

  return 'breadcrumbs'
}

interface OptionsType {
  page: Page,
}
