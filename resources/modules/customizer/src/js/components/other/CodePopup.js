import React, {Component} from "react";

/**
 * to implement a react-component that takes the following parameters:
 * 1.children - the child react-component, which is located inside the component and does not change in any way
 * 2. text - text for the popup
 * The function of this component is to show a popup with the text you pass as "text" in the parameters of the component when you hover over it. The height of the popup is 200px, width 400 pixels. The text style matches the Darcula theme from the PhpStorm IDE. The text is scrolled with the middle mouse wheel when the mouse is over the component. use the blueprintjs library for the popup. store the styles in a separate file in scss format.
 */
class CodePopup extends Component {

}

export default CodePopup
